<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vogella.android.myapplication"
    android:versionCode="1"
    android:versionName="1.0">

    <!--
         <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="15" />
    -->

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="false"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".activity.SignupActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".activity.ExpenseActivity" />
        <activity android:name=".activity.IncomeActivity" />
        <activity android:name=".activity.CalendarActivity" />
        <activity android:name=".activity.IncomeViewActivity" />
        <activity android:name=".activity.ProjectsViewActivity" />
        <activity android:name=".activity.ExpenseViewActivity" />
        <activity android:name=".activity.AddProjectActivity" />
        <activity android:name=".activity.AddFarmActivity" />
        <activity android:name=".activity.FarmsViewActivity"></activity>
    </application>

</manifest>